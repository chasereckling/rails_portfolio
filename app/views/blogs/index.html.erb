<% content_for(:navbar) do %>
<% end %>
<%= link_to "Add blog", new_blog_path(@blog), id: "new_link", remote: true %>

<h1>Blog Page</h1>
<div id="blogs">
  <% @blogs.each do |blog| %>
      <%= render blog %>
      <div id="<%= blog.id %>">
      </div>
    <% blog.comments.each do |comment| %>
      <% render comment %>
    <% end %>
  <% end %>
</div>
